查看当前存在位置:==pwd==

前往某一目录:==cd 绝对路径==(从“/”开始写),相对路径(开头不写”/“),（”cd --“回到家目录, cd ~/路径)

创建目录:==mkdir 目录名==

查看当前文件夹中所有文件和目录:==ls -a==, ==ls -l==, ==ll==(最常用,与ls -l等效)

复制粘贴:==cp 复制的文件名 粘贴的目录==   (批量复制:==cp -r  想要复制的文件夹 粘贴的目录==，这种复制方式也会把当前整个文件夹全部复制过去，若是只想要复制文件夹里的所有文件,可以使用==cp -r 目录/*  粘贴的目录==，如果存在相同文件名需要覆盖确认,可以改成==\cp ---==这样可以跳过确认操作)
![Pasted image 20260722153034](../Pasted%20image%2020260722153034.png)
![Pasted image 20260722153149](../Pasted%20image%2020260722153149.png)



创建文件:==touch 文件名==

删除文件:==rm 文件名==(不能删除目录)

移动文件:==mv 文件名 目录名/修改后的文件名==
![Pasted image 20260722163032](../Pasted%20image%2020260722163032.png)

查看文件: ==head -n 数字 文件名==  (从文件第一行开始查到第几行)                                 ==tail -n 数字 文件名== (从文件最后一行开始查到第几行)[还可以输入tail -f 文件名  实时追踪文件末尾新增内容]
![Pasted image 20260722181428](../Pasted%20image%2020260722181428.png)

echo:打印文字,类似于python中的print,其中/n为换行,/t为对其表结构
![Pasted image 20260722204023](../Pasted%20image%2020260722204023.png)


覆盖与追加:==内容(可以是echo +内容,也可以是查询结果) >(>>) 文件名==(”>“为覆盖文件内内容，”>>“在文件内容后追加内容)
![Pasted image 20260722204335](../Pasted%20image%2020260722204335.png)


连接:硬连接：==ln  绝对路径的文件名  创建连接的目录/连接名==(硬连接不常用，删除也只能删除连接名)
     软连接：ln -s 绝对路径的文件名  创建连接的目录  (通常将位置很深的文件提至前面,相当于windows系统中的快捷方式) ![Pasted image 20260722211325](../Pasted%20image%2020260722211325.png)
     